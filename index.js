var a = 900;
var b = 75;

function add(a, b) {
  return(a + b);
  }
add();

function subtract(a, b) {
  return(a - b);
  } 
subtract();

function multiply(a, b) {
  return(a * b);
  }
multiply();

function divide(a, b) {
  return(a / b);
  }
divide();

var n = 81;

function inc(n) {
  return(n + 1);
  }
inc();

function dec(n) {
  return(n - 1);
  }
dec();

function makeInt(n) {
  return parseInt('n', 10);
  }
makeInt();

function makeInt(n) {
   return parseInt(n, 10);
  }
makeInt();

function preserveDecimal(n) {
  return floatInt()
  }





